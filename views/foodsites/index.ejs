<% layout('layouts/boilerplate')%>
<div id="cluster-map"></div>
<div class="container my-5">
  <h1 class="text-info mx-6">Uploaded Foodsites</h1>

  <% for (let site of foodsites){%>

  <div class="card mb-3 cardbg">
    <div class="row">
      <div class="col-md-4">
        <%if(site.images.length) {%>
        <img class="img-fluid" alt="" src="<%=site.images[0].url%>" />
        <% }else {%>
        <img
          class="img-fluid"
          alt=""
          src="https://res.cloudinary.com/ddk6tmz8j/image/upload/v1648393529/FeedTheNeedy/close-up-of-a-full-platter-of-food_g4vquz.jpg"
        />
        <% } %>
      </div>
      <div class="col-md-8">
        <div class="card-body">
          <h5 class="card-title"><%= site.title %></h5>

          <p class="card-text"><%= site.description %></p>
          <p class="card-text">
            <small class="text-muted"><%= site.location%></small>
          </p>
          <a class="btn btn-primary" href="/foodsites/<%=site._id%>"
            >View <%=site.title%></a
          >
        </div>
      </div>
    </div>
  </div>
  <% }%>
</div>
<script>
  const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
  const foodsites = { features: <%- JSON.stringify(foodsites) %>}
</script>

<script src="/javascripts/clusterMap.js"></script>
